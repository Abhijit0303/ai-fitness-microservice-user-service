spring:
  application:
    name: activity-service
  data:
    mongodb:
      uri: mongodb+srv://Abhijit:Password@activity-service.ianpcfo.mongodb.net/?appName=activity-service
      database: activity-service
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

rabbitmq:
  exchange:
    name: fitness.exchange
  queue:
    name: activity.queue
  routing:
    key: activity.tracking
